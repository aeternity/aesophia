%% -*- mode: erlang -*-

{minimun_otp_vsn, "20.1"}.

{erl_opts, [debug_info]}.

{deps, []}.

{dialyzer, [
            {warnings, [unknown]},
            {plt_apps, all_deps},
            {base_plt_apps, [erts, kernel, stdlib]}
	   ]}.
